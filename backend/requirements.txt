# Production dependencies for Render deploys. Mirror of pyproject.toml's
# [project].dependencies, with psycopg[binary] added so alembic's sync
# migration engine can connect to the managed Postgres on boot.
#
# Regenerate by running:  uv export --format requirements-txt > requirements.txt
# (or update this file by hand when adding deps to pyproject.toml).

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic[email]>=2.9.0
pydantic-settings>=2.5.0
sqlalchemy>=2.0.35
alembic>=1.13.3
asyncpg>=0.29.0
psycopg[binary]>=3.2.0
aiosqlite>=0.20.0
argon2-cffi>=23.1.0
pyjwt>=2.9.0
python-multipart>=0.0.12
pypdf>=5.0.1
python-docx>=1.1.2
httpx>=0.27.2
tenacity>=9.0.0
structlog>=24.4.0
google-genai>=0.7.0
google-auth>=2.34.0
